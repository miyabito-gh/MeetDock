# AGENTS.md

## プロジェクト概要

- MeetDockは、会議グループと資料を管理し、Windows上のファイル・フォルダー・アプリ／ウィンドウを安全に起動・復元するTauriアプリである。
- フロントエンドの状態遷移と契約は主に `src/`、ネイティブ処理と永続化は主に `src-tauri/src/`、共有契約・回帰テストは `tests/` と `src-tauri/tests/` にある。
- JavaScriptとRustの境界は厳格なIPC契約である。片側の型・既定値・正規化・検証を変更する場合は、もう片側と共有フィクスチャへの影響を確認する。
- Windows固有APIは、正規化・照合・重複判定などの純粋処理と分離し、実機なしで単体テストできる境界を保つ。

## 基本方針

- ユーザーの依頼範囲を超えて実装・整理・リファクタリングを行わない。
- 調査依頼ではコードを変更しない。
- 不明点は推測で広げず、最小限の仮定で進める。
- 作業開始時に対象ファイルを限定する。
- 完了条件を満たした時点で停止する。
- 既存仕様の互換性を優先し、タイトルや表示名だけからパス・復元対象・Shellロケーションを推測しない。
- MeetDock自身が起動した資料との関連付け、Explorerの `existing_tab`、厳格なスナップショット／IPC検証を壊さない。

## 作業開始時の確認

- 最初に `git status --short`、対象範囲の `git diff`、必要に応じて `git log -5` を確認する。
- 既存の変更と未追跡ファイルはユーザーの作業として扱い、依頼対象と明確に分離する。
- `src-tauri/.gotodo/`、`src-tauri/gotodo.toml`、`src-tauri/todo.jsonl` は、ユーザーが明示的に対象へ含めない限り変更・削除・ステージしない。
- 実装前に、変更対象ファイル、原因候補、対応する対象テストを短く示す。

## 検索範囲

- まず `src/`、`src-tauri/src/`、`tests/` のみを対象にする。
- `node_modules/`、`dist/`、`artifacts/`、`src-tauri/target/`、`public/assets/` は明示的な依頼がない限り検索しない。
- 設計書は必要な章だけ読む。全文を読み直さない。
- `rg` を使う場合は必ず除外対象を指定する。

## タスク分割

- 依頼を「調査」「実装」「テスト」「ドキュメント更新」に分ける。
- ユーザーが実装を依頼していない場合、実装しない。
- まず原因候補と対象ファイルを提示し、広範囲の変更は行わない。
- 無関係な既存変更を修正・整理・コミットしない。
- 複数の独立した残項目がある場合も、ユーザーが選んだ項目だけを完了し、他項目へ自動的に進まない。

## 実装上の共通制約

- パス比較はWindows表記差（大文字小文字、`/` と `\\`、末尾区切り、`\\?\\`／`\\?\\UNC\\`）を正規化して行う。
- 完全パスを取得できる異なる文書は、同じアプリでも別対象として保持する。曖昧な対象を推測で統合しない。
- 設定・スナップショット・IPC入力では、未知フィールド、必須フィールド欠落、不正な列挙値、位置配列による構造体入力を拒否する厳格性を維持する。
- `#[serde(default)]` やJavaScript側の既定値補完を追加・変更する場合は、旧データの読込互換性と厳格検証の両方をテストする。
- 外部コマンドやOS APIへ渡す値は、保存済みIDから解決するか、検証済みの絶対パス／HTTPS URLに限定する。
- エラー応答やログへ、未検証のパス、文書内容、OS詳細を露出させない。
- UIの見た目や操作を変更する場合も、モデル→Effect→IPCの既存責務分離を維持する。

## 引継ぎ指示の作成

- ユーザーが「引継ぎ指示」または同等の依頼をした場合、作業を同一モデルで継続する効率と精度を評価する。
- 引継ぎ依頼では、対象作業の進捗・未解決事項・次の手順を、該当する既存の引継ぎ文書へ反映する。引継ぎ文書が複数ある場合は対象領域に対応する文書だけを更新し、新規文書は既存文書へ安全に追記できない場合に限る。
- 文書へ記録した次チャット用の引継ぎ指示文は、最終回答のチャットにもコピー可能なコードブロックとして必ず全文出力する。文書だけを更新して終了せず、文書版とチャット版の内容を一致させる。
- 調査、設計、実装、テスト、実機確認などで必要な推論量・ツール利用・継続性が大きく異なる場合は、無理に一つのチャットへまとめず、工程ごとに次のチャットへ分割する。
- 分割する場合は、次のチャットへそのまま貼り付けられる具体的な引継ぎプロンプトを出力する。プロンプトには、目的、完了条件、対象ファイル、既存実装・制約、実施済み確認、未解決事項、次に実行するテストを含める。
- 引継ぎプロンプトごとに、後述の「モデル選定」に従って推奨モデル、reasoning effort、短い推奨理由を明記する。
- 分割が不要な場合も、その判断と、単一チャットで進める理由を短く示す。

## モデル選定

- ユーザーがモデルまたは使用禁止モデルを明示した場合は、その指定を最優先する。
- 通常は、現在のCodex実行環境が提示する利用可能モデルと対応reasoning effortを正とし、モデル選定のたびにOpenAI公式サイトを確認しない。
- 既定の選択は次のとおりとする。
  - 複雑な設計、長期実装、複数領域の調整、難しい原因調査: `gpt-6-astra`。
  - 通常規模の実装、レビュー、RustとJavaScriptをまたぐ作業: `gpt-5.6-sol`。
  - コストと品質のバランスを優先する検証、定型実装、文書更新: `gpt-5.6-terra`。
  - 短い調査、大量の軽量確認、単純な機械的作業: `gpt-5.6-luna`。
- reasoning effortは、短い確認や定型作業を `low`、通常実装を `medium`、複雑な設計・原因調査を `high`、広範囲で失敗コストが高い作業だけを `xhigh` 以上の目安とする。
- 速度・コスト・精度・長い文脈の維持・ツール操作量を比較し、既定値より適切なモデルがある場合は理由を添えて変更する。
- OpenAI公式ドキュメントを確認するのは、次の場合に限定する。
  - ユーザーが最新情報、公式根拠、価格、提供状況、モデル比較を明示的に求めた場合。
  - 実行環境に対象モデルが表示されない、選択時にエラーになる、または利用可否・対応reasoning effortが不明な場合。
  - モデル体系の変更をAGENTS.mdへ反映する場合。
- 引継ぎ作成時は、この基準から選定理由を説明し、上記条件に該当しない限りモデル確認のためのWeb検索を行わない。

## 利用枠と作業量の節約

- 正確性とユーザー指定の完了条件を優先しつつ、各工程で必要な最小範囲を読む。まず該当行と前後の文脈を確認し、ファイル全文や長いログは原因を絞れない場合だけ取得する。
- コマンド出力は判断に必要な範囲へ制限する。大量のテスト結果は失敗箇所と最終件数を確認し、成功ログを繰り返し会話へ取り込まない。
- 対象テストは作業開始時に確認し、修正後は実装がまとまった時点で実行する。途中で再実行するのは関係する失敗の切り分けや新しい変更の検証が必要な場合に限る。指定された最終テストと必須検証は省略しない。
- モデルとreasoning effortは上の「モデル選定」に従って工程ごとに選ぶ。Git操作や定型文書更新へ複雑な原因調査用の設定を引き継がず、難しい非同期処理など必要な工程だけ推論量を上げる。ユーザーの指定があればそれを優先する。
- 長い作業で工程の目的や必要な推論量が変わる場合は、既存の引継ぎ規則に従って短い現状と次の手順を記録し、次チャットへ分ける。完了済みの調査ログや設計経緯を丸ごと再読しない。

## テスト

- 変更対象に対応するテストだけを最初に実行する。
- テスト失敗時は、関係する失敗だけを調査する。
- 全体テストは、対象テストが通った後に必要な場合だけ実行する。
- ビルドやUI検証は依頼された場合、または変更内容から必須の場合だけ行う。
- JavaScriptの対象テストは `node --test tests/<対象>.test.mjs`、全体テストは `npm.cmd test` を使う。
- Rustの対象テストは `cargo test --manifest-path src-tauri/Cargo.toml <テスト名>`、全体テストは `cargo test --manifest-path src-tauri/Cargo.toml` を使う。
- Rust変更後は `cargo check --manifest-path src-tauri/Cargo.toml`、コミット前は `git diff --check` を実行する。
- 契約変更では最低限、Rustの `src-tauri/tests/contracts.rs` とJavaScriptの `tests/contracts.test.mjs` を確認する。
- テスト失敗が既存問題に見える場合も再実行して原因を限定し、依頼範囲内なら修正し、範囲外なら根拠とともに未解決事項へ記載する。

## 実機UIと外部作用

- Tauriアプリ、開発サーバー、ブラウザー、Office、Explorer、Acrobat等の実機UIは、ユーザーの明示許可があるまで起動しない。
- 実機確認が必要な変更でも、先にモック可能な境界と自動テストを完成させる。
- ファイル削除、既存設定の上書き、外部送信、pushなど、復旧や外部影響を伴う操作は依頼範囲を再確認する。

## Git操作

- `git status`、`git diff`、`git log` などの読み取り操作は通常権限で行う。
- `git add`、`git commit`、`git push` など、`.git` 内部を書き換える操作は最初から権限昇格付きで実行する。
- `git add` と `git commit` は、ユーザーがその時点の変更について明示的に依頼した場合だけ行う。実装・修正・テストの依頼だけではコミット依頼とみなさず、変更は未コミットのまま報告する。
- ユーザーからコミットを明示的に依頼された場合、その依頼を承認として扱い、チャットで重ねて確認せずに必要な権限昇格を直接要求して実行する。
- 権限不足を確認するための通常権限での試行を先に行わない。
- コミット時は、ステージング対象を確認したうえで `git add` と `git commit` を同じ承認内で実行する。
- ユーザーが明示的に依頼した場合のみ `git push` を行う。
- コミットには依頼対象のファイルだけを含め、コミット直前に `git diff --cached --check` と `git diff --cached --stat` を確認する。
- コミット後は `git status --short` と `git log -1 --oneline --decorate` を確認し、未追跡・未コミットの既存作業が保持されていることを報告する。

## 出力

- 最終報告は「変更ファイル」「実行したテスト」「未解決事項」の3点に絞る。
- 大量のログや生成ファイルの内容を回答に貼らない。
- 作業を継続する前に、依頼範囲を超える場合は確認する。
