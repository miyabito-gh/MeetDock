# MeetDock 次セッション引継ぎ指示

## 2026-09-20 PID/HWNDセッション追跡の実装・確認結果

- `src-tauri/src/launcher.rs`で`ShellExecuteExW`を用いたセッション限定PID/HWND追跡を実装済み。共有Stateは`NativeLauncher`のClone間で共有され、プロセスハンドルはRAIIで解放する。
- PID/HWND/プロセス開始時刻/生存状態が一致する場合だけ前面化する。Explorer・子プロセスへの委譲、HWND未取得・消失、複数候補、終了、PID再利用の疑いは追跡不能として再度開く。
- PID/HWNDは設定JSON、資料DTO、通常IPCレスポンスへ追加していない。`launcher::tests`、`cargo check`、`git diff --check`は合格した。
- 手動確認済み: txt、PDF、フォルダ、HTMLファイル。Office文書は検証環境がなく未確認。URLは意図どおり追跡せず、毎回開く要求を出す。

## 次チャットの最優先実装：PID/HWNDセッション追跡

設計差分監査で、フォルダ起動時だけでなく一般の外部アプリ起動についても、MeetDockが起動したプロセスをセッション中だけ追跡する方針を確定した。Explorer限定の機能として実装しない。

### 実装方針

- MeetDock起点の外部プロセス起動で、取得可能なPIDと起動時HWNDをセッション内の揮発状態へ登録する。
- PID/HWND、起動時刻、実行ファイル識別などを組み合わせ、PID単独で資料の起動状態を確定しない。
- 設定JSONや永続化DTOへPID/HWNDを追加しない。アプリ終了時に追跡状態を破棄する。
- Explorerが既存プロセスへ要求を委譲する場合、PID/HWNDを取得できない場合、複数ウィンドウ・子プロセスの場合は`not_trackable`または`unknown`として扱い、推測で成功扱いにしない。
- 既存のID-only IPC境界、保存済み資料再解決、タイトル照合、前面化の失敗分類を維持する。
- COM/ROT、Restart Manager、タスクバー通知、診断ログは今回の実装へ混在させない。

### 調査・実装の順序

1. 最初は軽量モデルで、`src-tauri/src/launcher.rs`、`src-tauri/src/lib.rs`、`src-tauri/src/contracts.rs`、関連テストと設計書の該当章だけを確認し、候補の責務境界・必要API・最小変更ファイルを列挙する。
2. 軽量モデルの結果をもとに、実装候補を1～2案へ絞る。広範囲の全文再読や無関係なUI調査は行わない。
3. 実装判断、Win32 APIの安全性、テスト設計が必要になった段階だけ、より高品質なモデルへ切り替える。GPT-6 Astraは原則使用せず、必要性が明確な場合に限りユーザー確認を取る。
4. 実装前に`git status --short`と対象ファイルの差分を確認し、既存変更を破棄しない。
5. 実装後は対象Rustテスト、必要なら`cargo check`、`git diff --check`を実行する。Windows APIの実機挙動は自動テスト合格と分けて報告する。

### 完了条件

- MeetDockが起動した外部プロセスのPID/HWNDをセッション限定で保持できる。
- PID/HWNDが無効化・終了・再利用された場合に誤って既存資料を成功扱いしない。
- Explorerの既存プロセス委譲やHWND未取得を`not_trackable`／`unknown`として扱える。
- 永続設定、通常の資料DTO、ID-only IPCへ実行時PID/HWNDを漏らさない。
- 関連テストと未実施のWindows実機検証を引継ぎ資料へ記録する。

## 次チャットの最優先依頼: UIの情報階層と操作密度を洗練する

ユーザー確認済みの方針として、現在のWindowsデスクトップ向けUIを、Fluent 2およびPrimer系の高密度な業務UIを参考に整理する。装飾を増やすのではなく、操作位置の安定、状態の優先順位、一覧の走査性、アイコンの一貫性を改善すること。

### 優先して実装する内容

1. 上部ツールバーを役割ごとに整理する。
   - 左側は画面タイトル、未保存状態、保存／破棄を一つの編集領域として扱う。
   - 右側は「状態更新」「並べ替え」「資料を追加」を補助操作、「メイン資料を開く」を主要操作として配置する。
   - 操作群の間は薄い区切り線または12～16 px程度の余白で区切る。
   - 編集状態が変わっても右側ボタンの位置を移動させない。
2. 保存状態の表示を静かにする。
   - `Clean`では「保存済み」を常時表示せず、状態表示自体を隠す。
   - `Dirty`は黄色の点と「未保存」、`Saving`は進行表示と「保存中」、`Conflict`は赤い警告表示と「保存競合」にする。
   - 保存成功後の「変更を保存しました。」という持続通知は表示しない。エラーや利用者の対応が必要な通知は残す。
   - `Ctrl+S`は既存の`SaveRequested`経路へ集約し、資料編集・DnD確認・設定問題などのモーダル表示中は背後のdraftを誤保存しない。
3. 資料一覧の状態表示を軽量化する。
   - すべてを塗りつぶしピルにせず、通常状態は小さな点またはアイコン＋短いテキストにする。
   - 「利用可能」は緑の点、「未確認」は薄いグレーテキスト、エラーは赤い警告アイコン＋テキストを基本とする。
   - 色だけに依存せず、状態テキストとアクセシブル名を維持する。
4. 資料行の操作を明確にする。
   - ユーザー確認済みのMeetDock固有操作として、ファイル種別アイコンは`activate_or_launch`の入口にする。既存ウィンドウがあれば前面化し、見つからなければ外部アプリで起動する。
   - 「…」メニューにも「外部で開く」を残し、同じ保存済み`material_id`経路を利用する。
   - 操作欄は右端を起点に並べ、「…」を常に最右端、PDFではその左に「PDFプレビュー」を表示する。
   - PDF以外ではプレビューボタンを表示しない。
   - アイコンボタンには具体的な`aria-label`とツールチップを付ける。
5. 検索中の結果表示を単一一覧へまとめる。
   - 通常表示では従来どおりメイン／参考セクションを維持する。
   - 検索中はメイン／参考へ分断せず、一覧順の単一リストとして表示する。
   - 検索結果では所属グループと区分を列または小さなラベルで示す。
   - 既存の2,000件検索100 ms目標を後退させない。
6. PDFプレビューのヘッダーとツールバーを整理する。
   - 1段目はPDF切替、全画面、閉じる。2段目はページ移動、ページ直接入力、倍率、幅合わせとする。
   - 正常表示時の「外部アプリで開く」はオーバーフローへ移してもよいが、表示不能時は主要な退避ボタンとして明示する。
   - ページ入力、PDF前後移動、直接選択、最大化、幅変更、高速切替時のgeneration guardを後退させない。
7. 空状態を追加する。
   - 資料がない場合は、理由と「資料を追加」またはドラッグ＆ドロップの次操作を小さく案内する。
   - 新しいモーダルや大きなイラストは追加しない。
8. アイコン体系を統一する。
   - 絵文字、英字、Unicode記号の混在を減らし、16 px程度の単色SVGアイコンへ揃える。
   - Windowsとの親和性を優先するならFluent System Icons、現状の簡潔なUIとの整合を優先するならOcticonsを候補とする。
   - 外部起動、PDFプレビュー、フォルダー、URL、並べ替え、全画面、その他の意味が重複しないこと。

### 実装順

最初の変更単位では、次の3点をまとめて行う。

1. `Clean`時の保存状態表示を隠し、未保存／保存中／競合だけ表示する。
2. ツールバーを編集操作、補助操作、主要操作の3群へ視覚的に分ける。
3. 資料一覧の状態表示と行アクションを軽量化する。

その後、検索結果の単一一覧化、PDFヘッダー整理、空状態、SVGアイコン統一を独立した変更単位で進める。既存差分を破棄せず、各単位で関連JavaScriptテストとproduction buildを実行する。

### 今回のチャットで実装済みのため後退させない内容

- `Clean`時の保存状態は非表示。`Dirty`／`Saving`／`Conflict`だけをタイトル横へ表示する。
- ツールバーは編集操作、補助操作、主要操作の3群へ分割し、編集状態が変わっても右側操作の位置を動かさない。
- 資料状態は塗りつぶしピルではなく、利用可能の緑点、未確認の灰色点、エラーの赤い警告表示とテキストで示す。
- ファイル種別アイコンは`activate_or_launch`操作を兼ねる。ツールチップと`aria-label`で、既存ウィンドウの前面化／未起動時の外部起動を明示する。
- 行操作は右端起点で詰め、「…」を最右端へ固定する。PDFプレビューはPDF行だけ、その左に表示する。「…」メニューには「外部で開く」を残す。
- PDF行の常設プレビューボタン、表示中行の`aria-current`と視覚表示。
- 現在の一覧文脈に基づくPDF前後移動と直接選択。通常時は選択グループ内、検索中は検索結果内を対象とする。
- PDFページ番号の直接入力と範囲検証。
- PDFプレビューペインの通常表示時最大幅を800 pxから1,200 pxへ拡張。
- PDF切替時のID-only Effect、generation guard、render cancel、Canvas初期化、cleanup／destroy順。
- 左サイドバーの選択グループ表示は太い左線を使わず、薄い背景、細い枠、濃い文字で表現する。
- PDFプレビューアイコンは目玉ではなく、現在は分割パネル風の記号を使用している。次回SVG統一時に置換してよい。
- 未保存の浮動ポップアップは廃止済み。保存／破棄はタイトル側、右側の状態更新／並べ替え等は位置が動かない構造にしている。
- `Ctrl+S`保存と、モーダル表示中の誤保存防止を実装済み。
- 保存成功後の「変更を保存しました。」という持続通知は抑止済み。
- PDF本文検索を実装済み。大文字・小文字を区別しない一致件数、現在位置、前後の一致への循環移動、一致ページへの遷移を提供する。
- PDF検索欄は通常非表示。PDFツールバーの「検索」または`Ctrl+F`で開き、閉じるボタンまたは`Esc`で閉じる。検索欄が開いている場合、`Esc`でPDF全体を先に閉じない。
- PDF本文検索はPDF世代と検索世代を照合し、PDF切替や古い検索完了が現在の表示を上書きしない。スキャンPDFのOCRと一致箇所のハイライトは未実装。
- PDFペイン幅420 px以下ではcontainer queryでヘッダー、ページ／倍率操作、検索操作を折り返し、280 pxまでボタン文字をはみ出させない。

PDF本文検索追加後の全JavaScriptテスト530件、狭幅レイアウト変更後の関連Viewテスト21件、Vite production build、`git diff --check`が成功した。Windows 11／WebView2での100%／200%表示、長い名称、PDFペイン280～420 px、620／720／900／1,100 px付近の配置は手動受入として残る。

### 自動テストと手動受入

- `Clean`時に保存状態表示が隠れ、`Dirty`／`Saving`／`Conflict`だけが適切に表示される。
- 編集状態が変化しても右側ツールバー操作の位置が変わらない。
- `Ctrl+S`が1回だけ保存を要求し、モーダル表示中は保存しない。
- 状態表示が色だけに依存せず、スクリーンリーダーから判別できる。
- 行アクションがキーボードで到達・実行でき、アイコンに具体的なツールチップと読み上げ名がある。
- 検索中の単一一覧がグループ名と区分を示し、PDF切替対象も同じ検索文脈を使う。
- 620 px、720 px、900 px、1,100 px付近のレスポンシブ境界で操作が重ならない。
- Windows 11/WebView2で100%／200%表示、長いグループ名・資料名、PDF最大化中を手動確認する。
- 変更箇所のJavaScriptテスト、Vite production build、`git diff --check`を実行する。広範な契約変更がなければ毎回の全テストは不要。

## 次チャットの依頼: PDFプレビュー内の資料切替UX改善

PDFプレビューを表示したまま、別のPDFへ少ない操作で切り替えられるようにする。現状は資料ごとに操作メニューを開き、「アプリ内でPDF表示」を押す必要があり、連続してPDFを確認しづらい。

### 推奨する操作・UI設計

1. 資料一覧のPDF行へ、常時見える「プレビュー」アイコンボタンを配置する。
   - オーバーフローメニューを開かず、1クリックでPDFを表示・切替できるようにする。
   - 外部アプリで開く操作とはアイコン、ラベル、ツールチップを明確に分ける。
   - キーボードでもTab移動とEnter／Spaceで実行できるようにする。
2. プレビュー見出しへ、現在表示中のPDF名と「前のPDF」「次のPDF」ボタンを配置する。
   - 対象は現在の一覧文脈に表示されているPDFとする。通常表示では選択グループ内、検索中は検索結果内のPDFを一覧順に移動する。
   - 先頭／末尾では該当方向をdisabledにし、循環移動は行わない。
   - PDFが1件だけの場合は両方disabledとする。
3. 見出しのPDF名を選択コントロールとして扱い、同じ対象集合から直接PDFを選べるようにする。
   - ネイティブ`select`または同等のアクセシブルなリストを優先し、長い名称は省略表示とツールチップを併用する。
   - 前後ボタンと直接選択は同じPresenterイベントへ正規化する。
4. PDF行を選択したときは現在表示中の行を視覚的に示す。
   - `aria-current`等も設定し、色だけに依存しない。
   - 一覧のスクロール位置、検索語、プレビューの最大化状態、ペイン幅はPDF切替で変更しない。

### 挙動

- 別PDFへの切替開始直後に、ページ表示を`— / —`、倍率を`—%`へ初期化し、読込完了後に`1 / 総ページ数`、`100%`を表示する。
- 前後PDF移動、直接選択、一覧のプレビューボタンはすべて既存の`PdfOpenRequested`相当の経路へ集約する。
- 高速連打時も既存のgeneration guard、render cancel、Canvas初期化、cleanup、destroy順を維持し、最後に選んだPDFだけを表示する。
- 読込失敗やパスワード要求時も前後PDF移動と直接選択は利用可能にし、別PDFへ退避できるようにする。
- 現在ページ、総ページ数、倍率、ページ移動、拡大縮小、幅合わせ、Ctrl＋ホイール、ドラッグパン、最大化、ペインリサイズを後退させない。
- 任意パスをフロントエンドや実行系IPCへ追加せず、保存済み`material_id`だけを使用する。

### 責務境界

- ViewはPDF候補一覧・現在位置・活性状態の表示と入力通知だけを担当する。
- Presenterは前後移動／直接選択／一覧ボタンの入力を型付けイベントへ変換する。
- 現在の一覧文脈から切替対象PDFと前後位置を決める処理はMediator側へ置く。
- PDF.js固有のload、page、scale、render状態は引き続き`PdfViewAdapter`だけが扱う。
- Effect Runner、generation guard、ID-only境界を維持する。

### 自動テストへ追加する内容

- PDF行の常設プレビューボタンから1操作で表示できる。
- 選択グループ内で前／次のPDFへ一覧順に切り替わる。
- 検索中は検索結果内のPDFだけを前後移動する。
- 先頭／末尾およびPDF 1件時のボタン活性状態が正しい。
- 直接選択で指定した`material_id`だけが`ReplacePdf`へ渡る。
- 切替開始時に旧ページ数・倍率が消え、完了後に新PDFの値へ初期化される。
- 高速切替時に古い世代の完了通知が最後に選んだPDFのUIを上書きしない。
- 読込失敗／パスワード要求状態からも別PDFへ切り替えられる。
- 任意パスがEffect／IPCへ流れない。

実装後は変更箇所のJavaScriptテスト、Vite production build、`cargo check`、`git diff --check`を実行する。Windows/WebView2での連続切替、キーボード操作、長い名称、最大化中の切替は手動受入として確認する。

## 次回の最優先作業（2026-09-19 設計差分監査）

ユーザー指示により、次チャットでは設計書と異なる次の実装を修正する。既存差分を破棄せず、優先順に対応すること。

1. ネイティブDnD登録を設計へ合わせる。（自動試験まで実装済み、Windows実機受入待ち）
   - OSパスをRust側で正規化・検証してから登録候補を返す。
   - 特に複数ファイルでは一括確認画面を表示し、区分と登録先グループを確定してからdraftへ追加する。
   - 現状は`src/presenter.js`でパスを直接`reference/file`資料へ変換している。
   - 設計根拠: 同設計書8.4。
2. PDF表示不能時の外部アプリ起動導線を追加する。（自動試験まで実装済み、Windows実機受入待ち）
   - `PDF_FALLBACK_TOO_LARGE`、非対応暗号方式、必要に応じて読取不能時に、登録済み`material_id`経由で外部起動できるボタンを表示する。
   - 任意パスをフロントエンドから実行IPCへ渡さない。
   - 設計根拠: 同設計書5.1、PDF-04。
3. PDFの現在ページ／総ページ数／倍率表示は自動試験まで実装済み。Windows/WebView2実機受入を残す。
4. 診断ログのローテーションと利用者による明示エクスポートを実装する。設計書7.3の機密情報非記録条件を維持する。

## 2026-09-19 今回完了

- 一括起動を保存済み資料IDの逐次実行へ変更し、資料間250 ms、未着手項目の取消、成功／失敗件数と資料別明細を実装した。Rust側の既存`batch_launch_main`にも250 ms間隔を追加した。
- 検索結果の各資料行へ所属グループ名を表示した。
- PDF表示中の`Esc`終了を、資料編集モーダル・設定問題モーダルの後の優先順位で追加した。
- 状態同期の成功時刻を保持し、最終同期時刻として表示した。
- 関連JavaScriptテスト100件、Rustの対象コンパイル、production build、`git diff --check`が成功した。全テストは実行していない。

次の項目も設計未達だが、既存の延期判断と混同しないこと。

- 二重起動時に既存ウィンドウの前面化が拒否された場合のタスクバー通知。
- Office COM/ROT、Restart Manager、両者を統合したウィンドウ特定はユーザー判断で検証環境確保後へ延期済み。
- Windows/PDF/UNC/原子的置換およびUI-03の実機受入は未合格のまま。

直近で実装済みのため、次回作業で後退させないこと。

- PDFプレビューのWebView2 URL解決、protocol CORS/OPTIONS、CSP修正。
- `Ctrl + マウスホイール`によるPDF拡大縮小、左ドラッグによるパン。
- Canvasの`max-width: 100%`制約を解除済み。100%超（adapter上限300%）では横スクロール／ドラッグ可能。
- PDF本文検索、検索結果の世代guard、利用時のみ開く検索欄、PDFペイン幅420 px以下のcontainer queryによる折り返しを実装済み。
- フォーカス復帰時の自動状態同期（2秒クールダウン）。手動の「状態を更新」は残す。
- 汎用メッセージ「操作結果を確認してください。」は廃止済み。
- `.editbar[hidden]`の表示不具合は`src/visibility.css`で修正済み。

確認方針: ユーザー指示により毎回の全テストは不要。変更箇所に直接関係する最小限のJavaScript/Rustテストとproduction buildを実施し、広範囲な契約変更時だけ全テストを行う。直近ではPDF本文検索追加後の全530件、狭幅レイアウト変更後のView関連21件、production buildが合格している。

MeetDockの実装をPhase 7のWindows/WebView2実機確認から継続してください。

## 作業開始時

1. `git status --short`と`git log -5 --oneline`を確認し、既存差分を破棄しないこと。
2. 現在のPhase 5第一段コミットは`3db3cdc feat: add bounded native launch commands (WIN-04 WIN-06)`。
3. 次の正本を上から順に読むこと。
   - `IMPLEMENTATION_HANDOFF.md`
   - `IPC_CONTRACT.md`
   - `MEDIATOR_STATE_TRANSITIONS.md`
   - `ARCHITECTURE_RESPONSIBILITY.md`
   - `PDF_RANGE_VERIFICATION.md`
   - `ACCEPTANCE_TEST_ASSIGNMENT.md`
   - `MeetDock 基本・詳細設計書.md`
4. 実装済み内容は`PHASE_0_2_IMPLEMENTATION.md`、`PHASE_3_IMPLEMENTATION.md`、`PHASE_4_IMPLEMENTATION.md`、`PHASE_5_IMPLEMENTATION.md`、`PHASE_6_IMPLEMENTATION.md`で確認すること。

## 現在の状態

- Phase 0～4は実装・自動試験済み。
- Phase 5第一段では`activate_or_launch`、`batch_launch_main`、`open_containing_folder`、保存済みID再解決、https/file/folder起動port、リテラルタイトル照合、制約付き前面化、一括継続を実装した。
- WIN-04～06と前面化失敗分岐のモック試験、strict envelope、local/main限定permission、ID-only DTO試験は合格している。
- Office ROTとRestart Managerは未実装。この端末にMicrosoft 365 x64の検証環境がないため、ユーザー判断で実機環境確保後へ延期した。Phase 5全体およびWIN-01～03を合格扱いにしないこと。
- PATH-03～05のACL/SMB実機受入も環境未登録のため未合格。
- 最新検証ではRust 43テスト、JavaScript 489テスト、Vite production build、`cargo check`、`git diff --check`が成功している。
- JavaScript側にはMediator、固定Event Chain、Effect Runner、IPC adapterとPDF切替adapterがある。古い非同期応答で現在表示を上書きしない契約を維持すること。

## Phase 6完了状況

`PHASE_6_IMPLEMENTATION.md`を参照してください。protocol、PDF.js adapter、Worker、候補CSPと自動試験は実装済みです。PDF-01/02/04/05およびWorker/CSPはWebView2実機未検証のため合格扱いにしません。

## Phase 7の現在地

`PHASE_7_IMPLEMENTATION.md`を参照してください。グループ／資料CRUD、階層とコンテキストメニュー、schema 3連番、ID-only保存場所表示、TauriネイティブDnD登録、PDF操作、可変レイアウト、設定問題UIと自動試験は実装済みです。次を実機で確認すること。

1. Windows 11/WebView2でキーボード操作、200%表示、長い名称を確認する。
2. 実データ2,000件の検索描画が100 ms目標を満たすか計測する。
3. PDF Canvas/password UIとproduction CSPを実機確認する。
4. Windows Explorerからfileをドロップし、draft追加、既存設定検証を通る保存、失敗時の入力保持を確認する。
5. PDFのページ移動、拡大縮小、幅合わせ、最大化、280～800 pxリサイズと高速切替時のcleanupを確認する。

完了条件はSEC-01、UI-01～03と自動UI試験の合格、主要操作のキーボード完結である。Phase 5延期項目やPhase 6実機受入を合格扱いにせず、UI変更へOffice/RM実装を混在させないこと。
