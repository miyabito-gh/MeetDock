# Phase 3 設定永続化 実装記録

実施日: 2026-09-19 / 一次実装・自動試験: Codex。
独立合否レビュー、実機障害受入: **未実施**。CONDITIONAL PASSを維持。配布・リリース不可。

## 作業基準とコミット

- 開始時: ローカルmaster、HEAD `105b57da5e847162f20b33a9f4f7e939518a2c94`、作業ツリーclean、origin/masterより4コミット先。
- 指定11文書と既存実装を確認。masterを継続使用し、履歴巻戻し・ブランチ/worktree移動・既存差分破棄なし。pushなし。
- `388eb6e feat: validate complete schema 3 invariants (CFG-03 SEC-02)`
- `1efe68a feat: persist settings with explicit recovery and strict IPC (CFG-02 CFG-04 CFG-05)`
- 本記録・CFG-01ハーネスを含むコミット: `test: prepare isolated settings fault harness and handoff (CFG-01 CFG-05)`（自己参照hashは記載しない）。

## 変更ファイル

| ファイル | 変更 |
| --- | --- |
| `src-tauri/src/contracts.rs`、`src/contracts.js` | schema 3業務検証、revision checked_add |
| `tests/generate-fixtures.mjs`、`tests/fixtures/contracts.json` | 共通fixture 344→392件 |
| `src-tauri/src/settings.rs` | ConfigManager、Mutex、revision、load/save/resolve、候補レジストリ |
| `src-tauri/src/settings_io.rs` | 注入可能なFileOps/Stage、sync/ReplaceFileW/MoveFileExW、UTC/一意token |
| `src-tauri/src/lib.rs` | main限定raw IPC envelope検証、3command登録、composition、IPC/権限試験 |
| `src-tauri/Cargo.toml` | 既存windows crateのFileSystem/SystemInformation feature追加。依存/lockfile追加なし |
| `src-tauri/build.rs`、`src-tauri/capabilities/default.json` | app command ACL生成・local/mainだけへ許可 |
| `src-tauri/permissions/autogenerated/{health_check,load_settings,resolve_settings_issue,save_settings}.toml` | Tauri生成permission |
| `src-tauri/gen/schemas/{acl-manifests,capabilities,desktop-schema,windows-schema}.json` | 上記の生成結果 |
| `src-tauri/tests/settings.rs` | 26永続化統合試験（工程表の各ケースを含む） |
| `src-tauri/examples/cfg01.rs`、`CFG_01_VERIFICATION.md` | 専用生成fixture、工程停止・部分書込ハーネス、実機手順 |
| `IPC_CONTRACT.md`、`MeetDock 基本・詳細設計書.md` | Phase 3契約補足、順序例の訂正、OS置換失敗保証の訂正 |
| `IMPLEMENTATION_HANDOFF.md`、`PROJECT_HANDOVER.md`、`ACCEPTANCE_TEST_ASSIGNMENT.md`、本書 | 現状と未検証項目の引継ぎ |

## 実装済みの契約

- Rust/JS双方でID重複、孤立親/資料、自己参照・循環、空名称、同一親/区分の連番順序、Windows絶対パス、https、128文字リテラルヒントを検証。深い木は再帰なしで走査。空ヒントだけnullへ正規化。正規表現照合なし。
- File/folderは字句検証のみ。ドライブ/UNC/対応するextended形式を許可し、device/ADS/相対/`.`/`..`/予約名/禁止文字等を拒否。UNC・資料パスへの実I/Oなし。
- ConfigManagerの非同期Mutex取得後にcurrentを再読込。config.revision=expected_revisionとディスクrevisionを検証。成功時だけ安全整数内で+1。日時・app_versionはRust確定。blocking I/Oの完了までMutexを保持する。
- 同一ディレクトリのランダムtempをcreate_newで作成、全書込→sync_all→再読込のbyte一致＋完全検証。valid currentだけbak.newへコピー/同期/検証し、bak2→3、bak1→2、bak.new→1をMoveFileExWのREPLACE_EXISTING/WRITE_THROUGHで移動。
- currentありはReplaceFileW（明示退避先付き）、初回だけMoveFileExW（上書きflagなし）。失敗時はtempを残し、CONFIG_IO等を返す。commit後のcleanup失敗で偽の保存失敗を返さない。
- 破損/欠落current時だけ完全検証済みbackup/tempを候補化。revision降順、実パスなし、選択時の再検証とbyte一致、古いID拒否。自動復元なし。
- currentの将来/旧schemaはDTO完全デコードより前に分岐し、config=nullのセッション読み取り専用。schema 1/2 migrationなし。legacyは新設定/復旧残存ファイルがない時だけ明示importし、元を保持する。
- load_settings/resolve_settings_issue/save_settingsはraw JSON→strict envelope→既存decode/Validate→ConfigManagerを通る。Serde型デコードで終わらない。候補IDから内部登録パスを解決する。
- アプリ独自commandのpermissionを明示化し、local/main限定。CSPは引き続きnull。shell権限追加なし。本番UI、PDF、起動/前面化、UNCワーカーは変更なし。

## 試験結果

| 実行 | 結果 |
| --- | --- |
| `npm test` | 484成功、失敗/skip 0（共通fixture392、既存Mediator/Event Chain等を含む） |
| `cargo test --manifest-path src-tauri/Cargo.toml` | 30成功、失敗/ignore 0。IPC2、契約2（うち392fixture）、設定26 |
| `npm run build` | 成功。既存疎通画面のVite build。本番UI接続を検証したものではない |
| `cargo check --manifest-path src-tauri/Cargo.toml` | 成功 |
| `cargo check --manifest-path src-tauri/Cargo.toml --examples` | 成功 |
| `cargo build --manifest-path src-tauri/Cargo.toml --example cfg01` | 成功 |
| cfg01 seed → save → inspect | revision13/readyの通常smoke成功（実機障害試験ではない） |
| `cargo fmt --manifest-path src-tauri/Cargo.toml -- --check` / `git diff --check` | 成功 |

共有fixture SHA-256: `7802360EA332090DF1070D72688CE41A72A46A8567179F30E18EF48F6F254829`。seedは既存`meetdock-contract-v1`を維持。

自動試験は新規の`%TEMP%/meetdock-phase3-{GUID}`だけを使用し、既存設定は使用していない。全保存工程の事前失敗、temp/backup部分書込、検証可能だが内容不一致の再読込、revision競合、保存途中の外部変更、1177後のファイル配置シミュレーション、cleanup失敗、候補破損/差替え、legacy/未知schema、strict IPCを確認した。通常のWindows置換APIはこの統合試験とsmokeで呼び出しているが、NTFS障害受入を合格とする証跡ではない。

ローカルログ: `artifacts/phase-3-npm-test.log`、`artifacts/phase-3-cargo-test.log`、`artifacts/phase-3-harness.log`（gitignore対象）。Vite build初回はesbuildの親ディレクトリ参照がsandboxで拒否された。同一コマンドを承認された権限で再実行し成功。MSVCリンク時のライブラリ生成warningは試験失敗なし。コード変更による回避なし。

## 設計との差異・補足

1. 基本設計のJSON例のreference order=2/3を、同一区分内連番という規則に合わせ1/2へ訂正した。
2. ReplaceFileWは1177でcurrent名を維持できないため「失敗時に必ずcurrent名を残す」という無条件記述を訂正した。明示退避名と事前検証済みbackup/tempで内容を保持する。API仕様で非対応のREPLACEFILE_WRITE_THROUGHは使わない。OS障害中に旧パスを必ず復旧できるとは主張しない。[MicrosoftのAPI仕様](https://learn.microsoft.com/en-us/windows/win32/api/winbase/nf-winbase-replacefilew)を参照。
3. pristine初回の空設定revision0、復元/移行時のrevision維持、候補ID失効、復旧残存ファイルをlegacyより優先、session読み取り専用、commit後cleanupを補足。wire形式とenumは変更なし。独立レビュー未実施。
4. currentが有効なら残存tempが高revisionでもcurrentを採用する。破損/欠落時だけ候補提示する。保存失敗後の高revision tempを無断採用しない。
5. 外部変更はMutex内再読込とcommit直前比較で検出するが、他プロセスとの比較・置換をOSレベルで一体化するCASではない。single-instance前提を維持する。

## 未実施とPhase 4への引継ぎ

- CFG-01の工程別プロセス強制終了、ACL/共有違反、ストレージ/電源断の実機試験。手順・専用ハーネスを用意しただけで合格ではない。
- CFG-05の利用者操作・画面での手動復元、CFG-02/UI-02の製品UIでの編集保持確認（純粋モデル/IPC自動試験は成功）。本番UIへの接続は未実施。
- Phase 0〜2の補足経路と今回の設定契約補足の独立レビュー。特に1177時の候補復旧、初回/復元revision規則を確認する。
- PDF/WebView2、Office ROT/前面化、UNC/SMBの必須実機試験は引き続き未実施。M365/SMB環境、担当実名、レビュー者・証跡保存先の登録が必要。
- Phase 4のUNC 2ワーカー/64キュー/600ms/TTLはまだ未実装。今回の設定用blocking処理をUNC検査へ流用せず、専用固定ワーカーを実装する。
- Phase 4以降の保存済み設定によるID解決も同じ完全検証境界を維持する。実行系requestへ任意path/URLを追加しない。
- 配布相当buildでの必須受入、独立レビュー、プロジェクトオーナー承認までpush・配布・リリースなし。
